---
const { id, title, kicker = "" } = Astro.props;
---
<section id={id} class="section container">
  {kicker && <p class="chip mb-4">{kicker}</p>}
  <h2 class="h2 mb-8">{title}</h2>
  <slot />
</section>
